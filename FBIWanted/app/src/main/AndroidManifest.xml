<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/seal_of_the_federal_bureau_of_investigation_svg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/seal_of_the_federal_bureau_of_investigation_svg"
        android:supportsRtl="true"
        android:theme="@style/Theme.FBIWanted"
        tools:targetApi="31">
        <activity
            android:name=".feature.list.ListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.details.DetailsActivity"
            android:exported="false" />
    </application>

</manifest>